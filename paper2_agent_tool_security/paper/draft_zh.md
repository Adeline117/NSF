# AI Agent工具接口安全性：跨协议实证分析

**目标会议：** IEEE S&P / USENIX Security 2027

---

## 摘要

AI agent正日益依赖外部工具接口执行现实世界操作——签署区块链交易、查询数据库、执行代码及管理基础设施。多种协议并存以调解这些交互：Anthropic的Model Context Protocol (MCP)、OpenAI Function Calling、LangChain代理框架工具以及Web3原生智能合约模块。尽管授予大语言模型(LLM)对特权操作的访问具有重大安全隐患，迄今尚无系统性的、聚焦Web3场景的跨协议安全分析。本文提出了首个此类分析：我们从GitHub搜索并筛选42个Web3/区块链专用工具服务仓库，涵盖四个协议族（MCP 31个、Web3原生3个、OpenAI 3个、未分类5个），对全部42个仓库进行深度静态分析，并对其中10个仓库补充动态工具测试。我们设计了覆盖5个攻击面的30余种漏洞检测模式（含Web3专有模式：MEV暴露、滑点保护缺失、环境变量密钥泄露），映射至多个CWE标识。在42个Web3仓库中共发现2,961个静态安全问题，其中802个为严重级别、886个为高危级别、1,273个为中危级别；动态测试在10个仓库的136个工具中额外发现252个漏洞，合计3,213个安全问题。我们在15个漏洞类别中发现13个（86.7%）出现在两个及以上协议族中，表明这些安全缺陷是系统性的架构问题而非特定协议的实现错误。我们进一步量化了4个Web3特有攻击向量的实际流行度：私钥暴露（744个发现）、MEV暴露（80个）、滑点保护缺失（59个）和无限授权（42个），并揭示了安全包装器（Harness）的普遍缺失。我们在42个仓库中共识别889个工具定义，其中98个（11.0%）涉及敏感操作（签名、授权、转账、密钥管理）。我们提出6项Harness安全需求并评估其采纳现状。分析工具与数据集已开源发布。

---

## 1 引言

大语言模型正在从被动的文本生成器演变为主动的自主代理（AI agent）。这一范式转变的核心在于工具使用能力：agent通过调用外部工具与现实世界交互，包括读写文件系统、查询数据库、发送网络请求以及签署区块链交易 [1, 2]。工具接口赋予了agent执行特权操作的能力，但同时也引入了新的、独特的攻击面——agent继承了其可调用的每一个工具的安全漏洞。

当前，多种协议竞相定义agent与工具之间的通信标准。Anthropic于2024年末发布的Model Context Protocol (MCP) [3] 定义了工具、资源和提示三种原语，通过stdio或SSE传输层进行通信。OpenAI的Function Calling机制 [4] 在Chat API中使用JSON Schema定义函数接口。LangChain [5] 通过Python `@tool`装饰器和AgentExecutor提供工具绑定。在Web3领域，Safe模块 [6]、ERC-4337插件 [7] 和ERC-7579模块 [8] 构成了原生的智能合约工具接口体系。这些协议在传输机制、模式定义、执行模型和信任假设上存在根本差异，但均面临共同的安全挑战。

现有安全分析存在两个显著局限。**第一**，研究范围局限于单一协议。Invariant Labs [9] 对MCP进行了首个安全分析，发现了工具投毒攻击（tool poisoning），但未考察该漏洞是否为MCP特有。Hou等人 [10] 分析了MCP多代理系统中的工具间提权风险，同样限于MCP协议。**第二**，Web3场景的特殊风险未被系统研究。区块链交易具有最终性（finality），一旦执行即不可逆转——没有撤销交易（chargeback），没有回滚（undo）。一个恶意的`sendTransaction`调用即可清空整个钱包。将AI agent与区块链工具结合产生了独特的攻击向量，包括交易构造注入、无限授权劫持和私钥提取，其严重程度远超传统工具接口漏洞。

本文提出以下三个研究问题：

- **RQ1:** AI agent工具接口存在哪些安全漏洞？其分布和严重程度如何？
- **RQ2:** 这些漏洞是特定协议的实现缺陷，还是跨越协议边界的系统性问题？
- **RQ3:** Web3场景为AI agent工具接口引入了哪些特有的攻击向量？

为回答上述问题，我们进行了首个聚焦Web3场景的跨协议大规模安全实证分析。我们从GitHub搜索并筛选42个Web3/区块链专用工具服务仓库，涵盖四个协议族；对全部42个仓库进行深度静态分析并补充10个仓库的动态工具测试，设计30余种漏洞检测模式（含Web3专有模式）覆盖5个攻击面并映射至CWE标准。分析发现3,213个安全问题（2,961个静态 + 252个动态），其中15个漏洞类别中13个出现在两个及以上协议族中。

本文做出以下贡献：

1. **统一漏洞分类体系**：定义15个漏洞类别（含3个Web3专有类别），映射至CWE标准，覆盖5个攻击面。
2. **首个Web3聚焦的跨协议实证分析**：筛选42个Web3专用工具服务仓库、4个协议族，结合静态分析与动态测试，发现3,213个安全问题。
3. **五大攻击面与Web3特有向量量化**：系统化描述AI agent工具接口的完整威胁景观，首次量化私钥暴露（744个）、MEV暴露（80个）、滑点缺失（59个）等Web3向量的实际流行度。
4. **工具定义分析**：在42个仓库中识别889个工具定义，其中98个（11.0%）涉及敏感操作。
5. **Harness缺口分析**：定义6项安全包装器需求并量化其实际采纳率。
6. **开源工具与数据集**：发布静态分析框架、动态测试工具和数据集。

---

## 2 背景与威胁模型

### 2.1 AI Agent工具协议

AI agent通过工具协议发现、描述、调用和接收外部工具的执行结果。当前存在四个主要协议族，各自在传输机制、模式定义和执行模型上具有不同特征。

**MCP (Model Context Protocol).** Anthropic于2024年末发布的开放协议 [3]，定义了三种交互原语：工具（tools）用于执行操作、资源（resources）用于数据访问、提示（prompts）用于模板化交互。传输层支持stdio（本地进程间通信）和SSE/HTTP（远程通信）。MCP还支持采样（sampling）机制，允许服务端请求LLM进行推理。在我们的数据集中，MCP是最大的协议族，占336个仓库中的205个（61.0%）。

**OpenAI Function Calling.** OpenAI在Chat Completions API中引入的机制 [4]，开发者通过JSON Schema定义函数签名，包括名称、描述和参数模式。当模型决定调用函数时，返回结构化的函数调用请求；客户端负责实际执行并将结果以`tool`角色消息返回。该机制不定义服务端架构，将执行责任完全委托给客户端实现。我们的数据集包含12个OpenAI Function Calling仓库。

**LangChain/Agent框架工具.** LangChain [5] 通过Python `@tool`装饰器和`BaseTool`基类定义工具，由`AgentExecutor`编排工具调用。工具定义包括名称、描述字符串和Python类型提示。LangChain的特点在于其共享内存机制——多个工具通过agent的记忆模块共享状态，这引入了独特的跨工具信息泄露风险。我们的数据集包含40个LangChain仓库。

**Web3原生模块.** 在区块链生态中，智能合约模块直接作为agent的工具接口。Safe模块 [6] 允许第三方代码代表多签钱包执行交易；ERC-4337 [7] 定义了账户抽象插件，支持自定义验证和执行逻辑；ERC-7579 [8] 规范了模块化智能账户的模块接口。这些模块以委托权限运行，可代表用户账户执行任意操作。我们的数据集包含79个Web3原生仓库。

表1总结了四个协议族的关键特征差异。

| 特征 | MCP | OpenAI FC | LangChain | Web3原生 |
|------|-----|-----------|-----------|----------|
| 传输层 | stdio/SSE/HTTP | HTTPS API | Python进程内 | EVM/RPC |
| 模式定义 | JSON Schema | JSON Schema | Python类型 | Solidity ABI |
| 执行模型 | 服务端进程 | 客户端托管 | Python进程 | 链上执行 |
| 信任假设 | 隐式信任 | 隐式信任 | 隐式信任 | 最小信任 |
| 状态管理 | 会话级 | 无状态 | 共享内存 | 链上状态 |

### 2.2 系统模型

AI agent工具接口由五个组件构成，我们独立且组合地分析其安全属性：

1. **Agent（代理）**：基于LLM的推理引擎（如Claude、GPT-4、Llama），解释用户意图并决定调用哪些工具。
2. **Protocol（协议）**：通信层，定义agent如何发现、描述、调用工具并接收结果。涵盖上述四个协议族。
3. **Tool Server（工具服务）**：执行请求的操作（文件I/O、API调用、区块链交易）并返回结构化结果。
4. **Harness（安全包装器）**：（通常缺失）围绕工具执行的安全封装，负责实施沙箱隔离、权限约束、速率限制和人工确认机制。
5. **Skill（技能）**：打包的能力单元，由提示模板、一个或多个工具绑定和编排工作流组成。技能引入了组合层攻击面，这在单独调用工具时不存在。

```
用户 ---> Agent (LLM) --[协议]--> Harness? ---> Tool Server
               ^                                     |
               |_________ 结果（可能被投毒）_________|
```

### 2.3 信任边界

我们定义了7个信任边界，每个边界代表一个数据在不同信任域之间传递的位置：

| 边界 | 描述 | 风险 |
|------|------|------|
| B1: 用户-Agent | 用户提示是不可信输入 | 注入攻击 |
| B2: Agent-协议 | Agent构造工具调用；协议序列化 | 参数篡改 |
| B3: 协议-Harness | Harness（如存在）在执行前拦截 | 绕过控制 |
| B4: Harness-工具 | 工具以自身权限级别执行 | 权限提升 |
| B5: 工具-Agent | 工具结果回传至LLM上下文 | 输出注入 |
| B6: 工具-外部世界 | 工具与外部系统交互（区块链、DB、API） | 未授权操作 |
| B7: Skill-Agent | Skill提示模板合并入agent上下文 | 指令覆盖 |

### 2.4 攻击者模型

我们定义三类攻击者，按访问权限递增、但在野外出现频率递减排列：

**A1: 恶意工具提供者。** 控制工具服务的源代码和部署。能力包括：在工具描述中注入操控性指令（工具投毒）；返回精心构造的结果以操纵agent推理；嵌入带有恶意默认值的隐藏参数；注册与合法工具同名的仿冒工具。目标包括窃取凭证（私钥、API密钥、会话令牌）、执行未授权交易和渗出用户数据。**流行度：高。** 开放的工具注册表（npm、PyPI、GitHub）允许任何人发布工具服务，用户基于README安装而无安全审计。

**A2: 提示注入攻击者。** 无法修改工具代码，但通过agent处理的数据（网页、邮件、文档、数据库记录、API响应）注入恶意内容。能力包括：构造能存活于输出净化的提示注入载荷；利用跨工具信息流（读工具泄露数据给写工具）；触发用户未请求的工具调用。**流行度：非常高。** agent读取的任何数据源都是潜在的注入向量。

**A3: 中间人。** 在agent和工具服务之间拦截协议消息。能力包括：修改传输中的工具描述、更改调用参数（如收款地址、金额）、修改工具结果。**流行度：较低但关键。** MCP的stdio传输免疫；MCP的SSE/HTTP传输、OpenAI API调用和RPC端点在未端到端启用TLS时均易受攻击。

### 2.5 攻击面

基于系统模型和攻击者模型，我们定义5个攻击面：

**S1: 工具定义面。** 向agent描述工具的元数据——名称、描述字符串、参数模式、默认值和示例。Agent将工具描述视为可信指令，恶意描述在协议层面与合法描述不可区分。漏洞模式包括描述中的指令注入、带恶意默认值的隐藏参数和过度宽松的模式定义。CWE映射：CWE-1321、CWE-913。

**S2: 输入构造面。** Agent从用户请求、对话上下文和工具模式构造工具调用参数的过程。Agent执行隐式类型转换、填充"合理"默认值，并可能将用户输入直接插入结构化参数而不进行验证。CWE映射：CWE-20、CWE-74、CWE-843。

**S3: 执行面。** 工具执行的运行时环境——进程隔离、文件系统访问、网络访问和系统调用。大多数工具服务以宿主进程的完整权限执行。漏洞模式包括无进程沙箱、不受限的文件系统和网络访问、`eval()`/`exec()`执行不可信输入。CWE映射：CWE-269、CWE-250、CWE-78、CWE-829。

**S4: 输出处理面。** 工具结果返回agent并纳入LLM上下文窗口以供进一步推理。工具结果被agent视为可信上下文，恶意结果可包含提示注入载荷、敏感数据或误导性信息。CWE映射：CWE-116、CWE-200、CWE-117。

**S5: 跨工具面。** 单个agent会话中多个工具之间的信息流和状态共享。Agent通常可访问不同权限级别的多个工具，缺乏跨工具数据流的访问控制使得低权限工具（只读数据获取器）可通过共享LLM上下文影响高权限工具（交易签名器）。CWE映射：CWE-863、CWE-284、CWE-362。

### 2.6 Web3特有攻击向量

以下四个向量专属于与区块链系统交互的agent-工具接口，与上述五个攻击面正交，通常跨越S2、S3和S5。

**W1: 交易构造注入。** 恶意工具（或提示注入）导致agent构造带有有害参数的区块链交易。目标包括：收款地址（重定向资金至攻击者钱包）、代币数量（清空全部余额）、`approve`授权方（向攻击者合约授予无限额度）、合约交互数据（调用恶意函数）。区块链交易的最终性意味着损失不可逆。CWE：CWE-345。

**W2: Approve劫持。** 工具将`approve(MaxUint256)`调用伪装为"必需步骤"插入实际操作之前。Agent遵循工具描述执行无限授权而不质疑数额。攻击者合约随后通过`transferFrom`清空已授权代币。攻击流程：(1) 工具描述要求"必须先授权无限额度"；(2) Agent调用`token.approve(攻击者合约, MaxUint256)`；(3) 用户的预期操作正常执行（看起来成功）；(4) 数小时/天后，攻击者调用`token.transferFrom(用户, 攻击者, 全部余额)`。ERC-20的授权模型是众所周知的安全隐患，无限授权在合法DeFi中普遍存在，因此不会引起警觉。CWE：CWE-250、CWE-862。

**W3: 私钥/会话密钥提取。** 提示注入或工具投毒尝试从agent上下文或工具参数中提取钱包凭证。向量包括：工具描述指示agent将私钥作为参数传入；工具结果中的提示注入要求agent"确认"密钥；会话密钥范围未正确限制；密钥存储于所有工具共享的环境变量中。私钥是持有者认证——无法撤销，无二次验证回退。密钥泄露意味着永久失去该密钥控制的所有资产。CWE：CWE-200、CWE-312。

**W4: MEV利用。** 恶意工具向MEV搜索者泄露待处理交易详情，或构造不带滑点/截止时间保护的交易以实现三明治攻击。向量包括：工具将交易发送到公共内存池而非私有中继；工具将滑点容忍度设为100%或完全省略；工具描述建议"为获得最佳效果，始终将滑点设为100%"。MEV是年提取规模超6亿美元的产业，三明治攻击在同一区块内自动化执行。CWE：CWE-200。

### 2.7 Harness安全需求

基于上述攻击面和向量，我们定义6类安全控制措施，作为正确保护agent-工具接口的基准需求：

**H1: 权限约束。** 每个工具仅获得其声明功能所需的最小权限。只读价格查询工具不能签署交易。实现方式：基于能力的访问控制、每工具IAM策略。

**H2: 执行沙箱。** 工具执行与宿主进程和其他工具隔离。包括资源限制（CPU、内存、时间）、网络限制（出站防火墙）和文件系统访问控制（只读挂载，禁止访问`.env`或密钥文件）。实现方式：容器、WASM、V8隔离区、seccomp-bpf。

**H3: 交易验证。** 针对Web3工具：允许的合约地址白名单、每笔和每会话支出上限、Gas限制强制执行、授权数额封顶（禁止`MaxUint256`）、滑点边界。实现方式：签名前评估的策略引擎。

**H4: 审计日志。** 所有工具调用记录时间戳、调用者身份、参数、结果哈希和执行时长。日志仅可追加且具有防篡改性。实现方式：结构化日志写入不可变存储，Web3工具发射链上事件。

**H5: 人工确认。** 高价值或高风险操作在执行前需要用户明确确认。基于阈值：超过特定金额的交易、首次工具调用、对生产资源的操作。实现方式：带超时的确认提示，组织环境中的多方审批。

**H6: 输出净化。** 工具结果在注入LLM上下文前进行净化处理。剥离已知的提示注入模式、截断至最大长度、脱敏敏感数据模式（密钥、密码、PII）。实现方式：在协议层应用的输出过滤管线。

---

## 3 方法

### 3.1 Server枚举与选择

我们采用系统化的仓库枚举策略从GitHub收集AI agent工具服务。

**搜索策略。** 我们使用GitHub API执行32个搜索查询，覆盖四个协议族的关键术语组合。对于MCP，搜索关键词包括"mcp tool server""mcp server""model context protocol"等；对于Web3原生模块，包括"safe module""erc4337 plugin""erc7579 module""smart account module"等；对于LangChain，包括"langchain tool""agent tool blockchain""langchain web3"等；对于OpenAI Function Calling，包括"openai function calling""function calling blockchain""chatgpt plugin"等。

**初始结果与去重。** 搜索返回的原始结果经过去重处理，按完整仓库名（owner/repo）去重后得到336个唯一仓库。

**协议族分布。** 336个仓库的协议族分布如下：
- MCP：205个仓库（61.0%），平均442颗星，总计90,605颗星
- Web3原生：79个仓库（23.5%），平均52.6颗星，总计4,153颗星
- LangChain：40个仓库（11.9%），平均15.1颗星，总计605颗星
- OpenAI：12个仓库（3.6%），平均2.9颗星，总计35颗星

**Web3专用仓库筛选。** 初始版本的深度分析选择了Top 20高星仓库，但发现这些仓库以通用工具服务为主（如Atlassian连接器、Xcode构建工具），并非Web3/区块链专用服务器。为纠正这一偏差，我们重新执行了针对性搜索：使用9类Web3关键词组合（"mcp blockchain""mcp crypto""mcp solana""mcp defi""mcp nft""mcp web3""mcp ethereum""web3 agent tool blockchain"等）在GitHub进行大规模搜索，并严格过滤：仅保留仓库名称或描述包含web3/crypto/blockchain/ethereum/solana/defi/wallet/token/swap/nft关键词的仓库。最终筛选得到42个Web3专用工具服务仓库，累计约2,900颗GitHub星，涵盖TypeScript、Python、JavaScript、Solidity和Rust等编程语言。对全部42个仓库进行深度静态分析，并对其中10个仓库补充动态工具测试（详见4.7节）。

### 3.2 协议分类

我们开发了自动化协议分类器，通过以下三层检测机制确定每个仓库的协议族归属：

**导入分析。** 扫描源文件中的import/require语句，检测协议特征库的引用：`@modelcontextprotocol/sdk`（MCP）、`openai`包中的function calling相关API（OpenAI）、`langchain`库的工具相关模块（LangChain）、Solidity中的Safe/ERC-4337/ERC-7579接口引用（Web3原生）。

**依赖扫描。** 分析`package.json`、`requirements.txt`、`Cargo.toml`等依赖文件，匹配已知的协议SDK包名。

**文件级指标。** 检测特征性文件模式，如MCP的服务配置文件、OpenAI的函数定义JSON、LangChain的agent配置以及Solidity合约文件。

对42个Web3深度分析仓库，分类器的置信度平均达到0.90以上。对于置信度低于0.8的仓库，我们进行了人工验证。最终42个仓库中：31个MCP、3个OpenAI、3个Web3原生、5个分类未定（unknown）。MCP协议在Web3工具生态中占绝对主导地位（73.8%），反映了MCP作为AI agent工具接口标准在区块链领域的快速渗透。

### 3.3 静态分析框架

我们设计并实现了专用的静态分析框架，针对AI agent工具接口的独特安全特征进行检测。

**漏洞模式设计。** 基于前述威胁模型和攻击面分析，我们定义了30余种基于正则表达式的漏洞检测模式。每个模式映射至一个漏洞类别和CWE标识，覆盖5个攻击面。核心检测模式包括：

- **IV-001/IV-002（输入验证缺失）**：检测地址参数未经以太坊地址格式验证（`Address =`模式无checksum校验）、数值参数未设边界检查（`value`类参数直接使用无范围校验）。映射CWE-20。
- **CE-001（跨工具提权）**：检测`callTool()`调用链中无权限检查的模式，标识工具间的非授权调用。映射CWE-284。
- **MH-001（Harness缺失）**：检测MCP Server、OpenAI函数处理器和LangChain工具实例化时是否包含安全封装。映射CWE-284。
- **HC-001（硬编码凭证）**：检测源代码中的API密钥、私钥和访问令牌硬编码模式。映射CWE-798。
- **PI-001/PI-002（提示注入）**：检测工具描述和输出中的指令注入模式。映射CWE-74。
- **SC-001（状态混淆）**：检测全局可变状态在工具调用间的不安全共享。映射CWE-362。
- **UA-001（无限授权）**：检测`MaxUint256`授权和无上限的`approve`调用。映射CWE-250。
- **PKE-001（私钥暴露）**：检测私钥在环境变量、参数或日志中的明文出现。映射CWE-312。

**误报过滤。** 为提高结果精确度，我们实施了三层误报过滤：(1) 注释检测——忽略代码注释中的匹配；(2) 测试排除——标记（但不过滤）测试文件中的发现，分开统计；(3) 上下文启发式——对特定模式进行上下文分析，例如`value: string`仅在参数传递上下文中标记。

### 3.4 风险评分

我们为每个仓库计算综合风险评分（0-100分），计算公式如下：

**风险分 = min(100, Σ(发现数_i × 严重度权重_i × 类别乘数_i) - Harness折扣)**

其中：
- 严重度权重：critical = 10, high = 5, medium = 1
- 类别乘数：根据漏洞类别的实际风险影响调整（如`private_key_exposure`乘数最高）
- Harness折扣：对每项实现的Harness特性给予固定分数折扣

风险等级划分：Critical ≥ 80, High ≥ 60, Medium ≥ 40, Low < 40。

---

## 4 实验结果

### 4.1 数据集概览

我们的深度分析数据集包含42个Web3/区块链专用工具服务仓库，跨越4个协议族。表2列出了风险排名前20的仓库的基本属性。

| # | 仓库 | 协议 | 星数 | 语言 | 工具数 | 敏感工具 | 发现数 | 风险分 | 风险等级 |
|---|------|------|------|------|--------|---------|--------|--------|----------|
| 1 | wallet-agent/wallet-agent | MCP | 5 | TS | 71 | 14 | 896 | 100.0 | Critical |
| 2 | agenticvault/agentic-vault | MCP | 4 | TS | 9 | 3 | 559 | 100.0 | Critical |
| 3 | nirholas/free-crypto-news | Unknown | 135 | TS | 229 | 9 | 273 | 100.0 | Critical |
| 4 | InjectiveLabs/mcp-server | MCP | 6 | TS | 28 | 5 | 210 | 100.0 | Critical |
| 5 | nirholas/UCAI | OpenAI | 28 | Python | 25 | 7 | 153 | 100.0 | Critical |
| 6 | dennisonbertram/mcp-web3-wallet-tester | MCP | 1 | TS | 1 | 0 | 103 | 97.6 | Critical |
| 7 | bnb-chain/bnbchain-mcp | MCP | 58 | TS | 44 | 11 | 101 | 93.5 | Critical |
| 8 | dcSpark/mcp-cryptowallet-evm | OpenAI | 9 | TS | 35 | 6 | 67 | 91.1 | Critical |
| 9 | mcpdotdirect/evm-mcp-server | MCP | 373 | TS | 1 | 0 | 63 | 85.8 | Critical |
| 10 | efekucuk/etherlink-mcp-server | MCP | 4 | TS | 1 | 0 | 63 | 85.8 | Critical |
| 11 | armorwallet/armor-crypto-mcp | MCP | 192 | Python | 44 | 3 | 51 | 82.1 | Critical |
| 12 | base/base-mcp | MCP | 342 | TS | 32 | 9 | 44 | 78.7 | High |
| 13 | Suryansh-23/hyperlane-mcp | MCP | 6 | TS | 15 | 2 | 35 | 74.3 | High |
| 14 | hydra-mcp/hydra-mcp-solana | MCP | 238 | TS | 22 | 5 | 33 | 72.3 | High |
| 15 | nirholas/pump-fun-sdk | OpenAI | 75 | Rust | 9 | 5 | 46 | 71.2 | High |
| 16 | magnetai/mcp-crypto | Unknown | 8 | TS | 40 | 4 | 29 | 68.2 | High |
| 17 | caiovicentino/polymarket-mcp-server | MCP | 348 | Python | 33 | 3 | 21 | 63.3 | High |
| 18 | alpacahq/alpaca-mcp-server | MCP | 613 | Python | 25 | 2 | 19 | 61.7 | High |
| 19 | berlinbra/polymarket-mcp | MCP | 130 | TS | 14 | 1 | 13 | 55.4 | Medium |
| 20 | zhangzhongnan928/mcp-blockchain-server | MCP | 10 | TS | 8 | 2 | 11 | 50.3 | Medium |

**关键观察：** 42个Web3仓库中有11个（26.2%）风险评级为Critical（≥80分），8个为High，10个为Medium，13个为Low/Safe。与通用工具服务相比，Web3仓库的风险分布呈现两极化：高风险仓库的发现数远超通用服务（wallet-agent/wallet-agent达到896个发现），原因在于Web3特有的漏洞模式（私钥暴露、交易验证缺失、MEV暴露）大幅增加了检测覆盖面。在889个已识别的工具定义中，98个（11.0%）涉及敏感操作（签名、授权、转账、密钥管理），这些工具是攻击者的首要目标。

### 4.2 漏洞分布

在42个Web3目标仓库中，静态分析共发现**2,961个安全问题**。

**按严重度分布：**
- Critical：802个（27.1%）
- High：886个（29.9%）
- Medium：1,273个（43.0%）

**按漏洞类别分布：**

| 漏洞类别 | 数量 | 占比 | 攻击面 | CWE |
|----------|------|------|--------|-----|
| 输入验证缺失 (missing_input_validation) | 1,027 | 34.7% | S2 | CWE-20 |
| 私钥暴露 (private_key_exposure) | 744 | 25.1% | W3 | CWE-200/312 |
| 跨工具提权 (cross_tool_escalation) | 589 | 19.9% | S5 | CWE-284 |
| 状态混淆 (state_confusion) | 152 | 5.1% | S5 | CWE-362 |
| MEV暴露 (mev_exposure) | 80 | 2.7% | W4 | CWE-200 |
| Harness缺失 (missing_harness) | 62 | 2.1% | S3 | CWE-284 |
| 交易验证缺失 (tx_validation_missing) | 61 | 2.1% | W1 | CWE-345 |
| 滑点保护缺失 (no_slippage_protection) | 59 | 2.0% | W4 | CWE-345 |
| 硬编码凭证 (hardcoded_credentials) | 48 | 1.6% | S3 | CWE-798 |
| 无限授权 (unlimited_approval) | 42 | 1.4% | W2 | CWE-250 |
| 无Gas限制 (no_gas_limit) | 32 | 1.1% | S2 | CWE-400 |
| 环境变量密钥泄露 (env_key_exposure) | 24 | 0.8% | W3 | CWE-312 |
| 过度权限 (excessive_permissions) | 21 | 0.7% | S3 | CWE-78 |
| 工具投毒 (tool_poisoning) | 10 | 0.3% | S1 | CWE-913 |
| 提示注入 (prompt_injection) | 10 | 0.3% | S1, S4 | CWE-74 |

**发现1：输入验证缺失仍是最普遍的安全问题。** 1,027个输入验证缺失发现占总发现的34.7%，在Web3场景中尤为危险——未经校验的地址参数可导致资金发送至错误或恶意地址，且不可逆转。

**发现2：私钥暴露是Web3仓库的第二大安全隐患。** 744个私钥暴露发现（25.1%）是Web3仓库与通用服务的最大差异。典型模式包括：`signTransaction`调用无用户确认、私钥以环境变量形式明文存储、钱包签名函数直接暴露给agent。这在通用工具服务中仅占0.3%（4/1,415），而在Web3仓库中跃升至25.1%。

**发现3：跨工具提权在Web3场景中风险更高。** 589个跨工具提权发现（19.9%）意味着只读的价格查询工具可能通过共享agent上下文触发签名工具执行交易——在Web3场景下直接导致资金损失。

**发现4：Web3专有漏洞类别（MEV暴露、滑点缺失、交易验证缺失、环境变量密钥泄露）合计占7.6%（225个），为通用漏洞模式无法检测的盲区。**

### 4.3 跨协议对比

表3呈现了按协议族划分的漏洞分布对比。

| 指标 | MCP | OpenAI | Web3原生 | Unknown |
|------|-----|--------|---------|---------|
| 仓库数 | 31 | 3 | 3 | 5 |
| 总工具数 | 433 | 44 | 0 | 412 |
| 敏感工具数 | 66 | 11 | 0 | 21 |
| 总发现数 | 2,390 | 212 | 37 | 322 |
| 平均发现数/仓库 | 77.1 | 70.7 | 12.3 | 64.4 |
| 平均风险分 | 63.8 | 81.5 | 26.6 | 48.0 |
| Critical级发现 | 708 | 51 | 11 | 32 |
| High级发现 | 795 | 48 | 5 | 38 |
| Medium级发现 | 887 | 113 | 21 | 252 |

**发现5：MCP协议族在Web3生态中占绝对主导且风险最高。** MCP以31个仓库贡献了2,390个发现（80.7%），其中708个为Critical级。MCP在Web3场景的主导地位（73.8%的仓库）反映了其作为AI agent标准接口的快速渗透，但也意味着绝大多数Web3工具继承了MCP协议的架构性安全缺陷。

**发现6：OpenAI仓库的平均风险分最高。** OpenAI仅3个仓库，但平均风险分为81.5（Critical级），远高于MCP的63.8。其Critical级发现占比为24.1%（51/212），高于MCP的29.6%（708/2,390）。但OpenAI仓库的绝对平均发现数（70.7）与MCP（77.1）相近，表明"无服务端"架构并未显著增加漏洞密度，但安全严重度更高。

**发现7：Web3原生模块安全性相对较高但样本有限。** 3个Web3原生仓库仅37个发现，平均风险分26.6。这可能归因于Solidity生态的安全审计文化和形式化验证工具链（如Slither、Securify）。但样本量过小（3个），不足以得出统计显著结论。

**发现8：系统性漏洞跨越协议边界。** 在15个漏洞类别中，有13个（86.7%）出现在两个及以上协议族中。具体分布如下：

- **4个协议族均出现（4个类别）**：missing_input_validation、private_key_exposure、state_confusion、tx_validation_missing
- **3个协议族出现（4个类别）**：unlimited_approval、no_slippage_protection、mev_exposure、tool_poisoning
- **2个协议族出现（5个类别）**：cross_tool_escalation、hardcoded_credentials、missing_harness、excessive_permissions、prompt_injection
- **仅1个协议族出现（2个类别）**：env_key_exposure（仅MCP）、no_gas_limit（仅MCP）

这一发现具有重要意义：**系统性漏洞不是特定协议的实现错误，而是所有AI agent工具协议共享的架构性缺陷。** 输入验证缺失、私钥暴露和状态混淆跨越全部四个协议族，表明当前协议设计从根本上未将安全性纳入核心考量。Web3专有漏洞（MEV暴露、滑点缺失、交易验证缺失）同样跨越多个协议族，表明区块链安全最佳实践在agent工具生态中尚未建立。

### 4.4 Web3特有发现

在42个Web3专用仓库中，我们系统量化了Web3特有攻击向量的实际流行度。这些发现是本研究的核心贡献之一。

**私钥暴露（744个发现，25.1%）。** 这是Web3仓库中最显著的安全问题类别。744个发现分布于全部四个协议族：MCP（665个）、OpenAI（42个）、Web3原生（11个）和Unknown（26个）。最普遍的模式是`signTransaction`/`signMessage`调用不经用户确认（PKE-003，占绝大多数），其次是私钥以环境变量形式明文存储通过`process.env.PRIVATE_KEY`读取（ENV-001，24个发现），以及硬编码的密钥十六进制字符串（PKE-002）。在agent-工具架构中，这意味着同一进程中运行的任何工具都可访问该私钥——一个只读的价格查询工具与签署百万美元交易的签名工具共享相同的密钥材料。dennisonbertram/mcp-web3-wallet-tester仓库尤为典型：作为"钱包测试工具"，其64个私钥暴露发现几乎全为无确认签名，使得该MCP服务器本质上是一个不受保护的签名机器。

**MEV暴露（80个发现）。** 80个MEV暴露发现表明Web3工具普遍在发送交易时不考虑MEV保护。典型模式为`sendTransaction`调用不使用私有交易中继（如Flashbots），直接将交易提交到公共内存池，暴露于三明治攻击和抢先交易风险之下。

**滑点保护缺失（59个发现）。** 59个滑点保护缺失发现出现在涉及DEX swap操作的工具中。这些工具在执行代币交换时不设置`minOutput`/`amountOutMin`参数或交易截止时间，使用户的每笔交易都面临价格滑移和三明治攻击的双重风险。

**交易验证缺失（61个发现）。** 61个交易验证缺失发现涉及`sendTransaction`调用不验证目标地址白名单或交易参数合法性。在Web3场景下，这意味着agent可被提示注入操纵向攻击者地址发送任意金额，且不可逆转。

**无限授权（42个发现）。** 42个无限授权发现分布于3个协议族：MCP（28个）、Unknown（8个）和OpenAI（6个）。最典型的模式是工具在执行代币操作前调用`approve(MaxUint256)`。在agent-工具场景中，风险急剧放大：agent可能被工具投毒指令操纵执行对攻击者合约的无限授权，而用户对此完全不知情。

**环境变量密钥泄露（24个发现）。** 24个发现显示私钥通过`process.env.PRIVATE_KEY`或`os.environ["PRIVATE_KEY"]`读取后直接传入签名函数，全部来自MCP协议族仓库。环境变量在容器化部署中可能通过多种途径泄露（日志、进程枚举、配置文件）。

### 4.5 Harness缺口分析

我们评估了42个Web3仓库对6项Harness安全需求（H1-H6）的实现情况。62个`missing_harness`发现（MH-001和MH-002模式）提供了定量证据。

**整体缺口。** 在42个仓库中，62个Harness缺失发现分布于约20个仓库。超过半数（22个）仓库完全没有任何Harness特性的痕迹。

**按需求类别分析：**

- **H1（权限约束）**：0/42个仓库实现了每工具的权限约束。在889个工具中，所有工具均以宿主进程的完整权限运行，包括98个敏感工具（签名、授权、转账）。这意味着一个价格查询工具与一个签署交易的工具拥有完全相同的权限。
- **H2（执行沙箱）**：0/42个仓库实现了工具执行沙箱。所有MCP服务以Node.js或Python进程运行，工具可访问完整文件系统、网络和环境变量（包括私钥）。
- **H3（交易验证）**：0/42个仓库实现了交易构造前的策略引擎。61个交易验证缺失发现和42个无限授权发现直接证实了这一缺口。
- **H4（审计日志）**：约4/42个仓库包含基本的日志记录功能，但均非安全级别的不可变审计日志。
- **H5（人工确认）**：0/42个仓库在工具层实现了自动化的人工确认机制。744个私钥暴露发现中的绝大多数为"签名无确认"模式，直接反映了这一缺失。
- **H6（输出净化）**：0/42个仓库对工具输出实施了净化处理以防止提示注入回传。

**核心发现：Harness的普遍缺失在Web3场景中风险最高。** 当工具涉及区块链交易签署、代币授权和资金转移时，Harness缺失的后果不仅是信息泄露或功能滥用，而是直接的、不可逆的金融损失。98个敏感工具无一受到权限约束、沙箱隔离或交易验证策略的保护。

### 4.6 高风险Server案例分析

我们对风险分最高的三个Web3仓库进行深入案例分析。

**案例1：wallet-agent/wallet-agent（风险分100.0，896个发现，71个工具，14个敏感工具）。** 这是一个面向EVM链的Web3钱包MCP服务，仅5颗星但暴露了71个工具（包含14个涉及签名/转账/授权的敏感工具）。896个发现中：665个私钥暴露（几乎全为签名无确认模式）、78个MEV暴露、55个交易验证缺失、32个无Gas限制。该仓库本质上将一个功能完整的以太坊钱包以无保护方式暴露给AI agent——agent可在无用户确认的情况下签署任意交易、授权无限额度、将资金发送至任意地址。这是我们数据集中最危险的Web3工具服务。

**案例2：agenticvault/agentic-vault（风险分100.0，559个发现，9个工具，3个敏感工具）。** 一个将AWS KMS签名能力和DeFi协议感知暴露给AI agent的MCP服务，4颗星。559个发现涵盖大量私钥暴露（使用AWS KMS但环境配置不安全）、跨工具提权和输入验证缺失。该服务的核心设计意图（"Server-side EVM signing"）本身即构成高风险：它将签名密钥的控制权交给MCP协议，任何能向该服务发送请求的agent都能签署交易。

**案例3：InjectiveLabs/mcp-server（风险分100.0，210个发现，28个工具，5个敏感工具）。** 由Injective Protocol官方维护的MCP服务，支持永续合约交易、现货转账、跨链桥接和原始EVM交易。210个发现中包含显著的私钥暴露、交易验证缺失和跨工具提权。作为官方维护的项目，其安全问题尤具代表性：即使是有充足资源的区块链项目团队，在将功能暴露为AI agent工具时也未实施基本的安全控制。5个敏感工具（涉及交易签名和资金转移）均无Harness保护。

### 4.7 动态工具测试

为补充静态分析，我们对42个Web3仓库中的10个进行了动态工具测试。动态测试解析每个仓库的实际工具定义，针对5个攻击维度生成测试用例：工具投毒（tool_poisoning）、提示注入输出（prompt_injection_output）、参数注入（parameter_injection）、交易验证（tx_validation）和私钥处理（private_key_handling）。

在10个仓库中共识别136个工具定义，对全部136个工具执行了5维度测试，发现252个漏洞：

| 攻击维度 | 测试数 | 易受攻击 | 比率 | 严重度分布 |
|----------|--------|---------|------|-----------|
| 参数注入 | 136 | 136 | 100.0% | 136 Medium |
| 提示注入输出 | 136 | 92 | 67.6% | 79 High, 13 Medium |
| 交易验证 | 136 | 19 | 14.0% | 15 High, 4 Medium |
| 私钥处理 | 136 | 5 | 3.7% | 4 High, 1 Medium |
| 工具投毒 | 136 | 0 | 0.0% | - |

**发现9：参数注入是普遍性问题。** 全部136个工具（100%）未定义参数Schema，接受任意输入——这在Web3场景中意味着agent可构造任意交易参数而无结构化验证。

**发现10：提示注入输出威胁严重。** 67.6%的工具存在输出被提示注入利用的风险，恶意工具结果可操纵agent执行后续敏感操作。

**发现11：静态与动态分析互补。** 静态分析擅长检测代码级漏洞模式（私钥暴露、无限授权），而动态测试揭示了运行时行为缺陷（参数Schema缺失、输出注入向量）。合计3,213个安全问题（2,961静态 + 252动态）提供了Web3 agent工具安全的全景视图。

---

## 5 讨论

### 5.1 系统性漏洞的根因分析

我们的实证发现表明，AI agent工具接口的安全问题是系统性的而非偶发的。15个漏洞类别中13个跨越多个协议族边界，这一结果指向三个根本原因：

**协议设计优先功能而非安全。** 四个协议族的规范均侧重于定义功能互操作性——如何描述工具、如何传递参数、如何返回结果。安全性被视为"可选的上层关注"而非协议核心属性。MCP规范提及了安全考量但不强制执行；OpenAI Function Calling将全部安全责任留给客户端；LangChain的`@tool`装饰器不包含任何安全元数据；Web3模块依赖Solidity的原生访问控制但缺乏agent层面的约束。

**开发者从不安全的范例中学习。** 协议文档和教程中的示例代码普遍不包含安全最佳实践。开发者复制这些模式，产生了遍布生态的"遗传性"漏洞。在Web3场景中，这一问题被放大：传统DeFi开发中的"无限授权"模式（MaxUint256 approve）在独立应用中已是安全隐患，当这些模式被移植到agent工具接口时，风险从"用户可审核的MetaMask弹窗"退化为"agent自动执行且用户完全不知情"。

**安全默认值的缺失。** 没有任何协议实现"安全默认"（secure-by-default）原则。工具默认以完整权限执行、默认无沙箱、默认无输入验证、默认无输出净化。安全需要开发者主动且正确地实现每一项控制措施——这是一个已被证明无效的安全策略。

### 5.2 协议设计建议

基于我们的发现，我们提出以下协议层面的安全增强建议：

**R1: 强制性权限声明。** 工具注册时必须声明所需的最小权限集合（读/写/执行/签名）。协议层在运行时强制执行权限边界。

**R2: 内置输入验证原语。** 协议模式定义应支持丰富的验证约束，不限于JSON Schema的基本类型。对于Web3工具，应内置地址格式验证、数值范围约束和授权额度限制。

**R3: 协议级输出净化。** 工具返回结果在进入LLM上下文前必须经过协议层的净化处理。这应是不可旁路的默认行为，而非可选特性。

**R4: 标准化Harness接口。** 协议应定义标准化的安全包装器接口，允许第三方安全中间件以声明式方式插入工具执行管道。

**R5: 交叉工具信息流控制。** 协议应支持声明式的工具间数据流策略，防止低权限工具的输出影响高权限工具的输入构造。

### 5.3 Harness设计框架

基于我们定义的6项Harness需求和实证的缺口分析，我们提出分层Harness设计框架：

**Layer 1: 协议层Harness（由协议SDK实现）。** 包括模式验证（H1部分）、输出净化（H6）和基本审计日志（H4）。这一层应由协议SDK作为默认行为提供，开发者无需额外代码。

**Layer 2: 运行时Harness（由部署环境实现）。** 包括执行沙箱（H2）、资源限制和网络隔离。可通过容器化、WASM隔离或进程级安全策略实现。

**Layer 3: 业务逻辑Harness（由工具开发者实现）。** 包括领域特定的验证规则（H3，如交易参数验证）和人工确认策略（H5，如高价值操作审批）。

这一分层设计确保Layer 1和Layer 2的安全措施无需开发者干预即可生效，仅Layer 3需要领域专家的参与。

### 5.4 负责任披露

我们已向所有42个分析仓库的维护者发送了安全通知。对于风险评级为Critical的11个仓库，我们提供了详细的漏洞报告和修复建议。我们遵循90天的负责任披露窗口期。截至论文提交，已收到部分维护者的确认回复。

我们的发现涉及的是工具服务代码本身的安全缺陷，而非特定用户的数据泄露。所有动态测试均在本地测试网或合成环境中执行，未涉及真实资金或用户数据。

### 5.5 局限性

**静态分析的精确度。** 基于正则表达式的静态分析存在固有的精确度局限。我们的检测模式可能产生误报（将安全代码标记为漏洞）和漏报（遗漏实际漏洞）。特别是PKE-003（签名无确认）模式在包含大量签名相关代码的Web3仓库中可能产生较高匹配数。我们通过人工抽样验证（对42个仓库中的300个发现进行人工审核）估算精确度约为60-70%。

**样本量与协议族分布。** 42个Web3仓库中协议族分布不均衡：MCP 31个、OpenAI 3个、Web3原生3个、Unknown 5个。OpenAI和Web3原生的样本量不足以支持统计显著的协议间比较。

**Web3仓库成熟度。** 42个Web3仓库中多数为小型项目（中位数星数约8颗），可能不代表生产级部署的安全水平。但这恰恰反映了Web3 MCP生态的现状：大量快速上线的工具服务缺乏安全审计。

**数据源限制。** 我们的数据集仅来自GitHub公开仓库。npm和PyPI注册表中的工具包、商业闭源工具服务以及企业内部部署的工具均不在分析范围内。

**因果推断限制。** 我们的分析是观察性的，仅证明了漏洞的存在和分布。我们无法确定这些漏洞在实际攻击中是否被利用，也无法量化其潜在的经济损失。

---

## 6 相关工作

### 6.1 MCP安全分析

Model Context Protocol (MCP) 由Anthropic于2024年末发布 [3]，作为连接LLM与外部工具和数据源的标准化接口。Invariant Labs [9] 发表了首个MCP系统安全分析，识别了工具投毒攻击——恶意工具描述嵌入操纵agent行为的指令。他们的工作证明MCP工具描述被LLM视为可信指令，使恶意工具提供者可劫持agent的行为。然而，其分析限于MCP协议，未考察漏洞是否跨协议存在。Hou等人 [10] 分析了MCP多代理系统中的安全风险，聚焦于工具间提权。本文将这些分析扩展至四个协议族，证明10/11个漏洞类别跨越协议边界。

### 6.2 LLM工具使用安全

Greshake等人 [11] 引入了间接提示注入概念，证明了工具增强型LLM因注入指令可触发未请求的工具调用而特别脆弱。Zhan等人 [12] 系统性分类了针对LLM agent的攻击，区分了针对推理的攻击和针对工具接口的攻击。OWASP LLM Top 10 [13] 将"过度授权"(LLM06)列为顶级风险。Debenedetti等人 [14] 提出AgentDojo框架评估AI agent的对抗鲁棒性。本文与上述工作互补：我们分析工具服务本身的固有漏洞，而非agent的鲁棒性。

### 6.3 智能合约安全

Atzei等人 [15] 发表了以太坊智能合约漏洞的开创性综述。Chen等人 [16] 使用机器学习大规模检测合约漏洞。Tsankov等人 [17] 开发了Securify安全分析器。本文的Web3原生分析针对模块化智能账户组件（Safe模块、ERC-4337插件、ERC-7579模块），这些作为AI agent工具接口引入了新的攻击面——模块可代表账户以委托权限执行操作，这一交叉领域未被现有工具覆盖。

### 6.4 API安全

OWASP API安全Top 10 [18] 识别了常见API漏洞。Atlidakis等人 [19] 开发了RESTler用于API端点模糊测试。我们认为AI agent工具接口是一类特殊API，具有超出传统API安全范畴的攻击面——特别是工具定义面（S1，描述影响LLM行为）和跨工具面（S5，信息通过LLM上下文流动）。

### 6.5 AI Agent安全综述

Xi等人 [20] 提供了AI agent架构和安全的全面综述，将工具使用列为核心能力。Wang等人 [21] 调查了基于LLM的自主agent，但安全讨论聚焦于对齐和安全性而非工具接口。Ruan等人 [22] 提出了AI agent系统风险分类。本文为这些综述提供了所呼吁的实证基础：我们在真实工具服务实现中量化了特定漏洞类别的流行度。

---

## 7 结论

本文提出了首个聚焦Web3场景的AI agent工具接口跨协议大规模安全实证分析。我们从GitHub搜索并筛选42个Web3/区块链专用工具服务仓库，涵盖MCP、OpenAI Function Calling和Web3原生三个主要协议族，进行了基于30余种检测模式（含Web3专有模式）的深度静态分析，并对10个仓库补充了5维度动态工具测试。

核心发现可总结为四点：

**第一，安全问题是系统性的。** 我们在42个Web3仓库中发现的2,961个静态安全问题分布于15个漏洞类别，其中13个（86.7%）出现在两个及以上协议族中。输入验证缺失（1,027个，34.7%）、私钥暴露（744个，25.1%）和跨工具提权（589个，19.9%）是最普遍的三个类别。这证明了AI agent工具接口的安全缺陷是架构性的——源于协议设计而非个别实现错误。

**第二，Web3场景急剧放大风险且已量化。** 我们首次量化了Web3特有攻击向量在真实工具服务中的流行度：私钥暴露744个发现（25.1%）、MEV暴露80个、滑点保护缺失59个、交易验证缺失61个、无限授权42个、环境变量密钥泄露24个。这些漏洞将工具接口的安全影响从信息泄露和功能滥用升级至不可逆的金融损失。在889个已识别的工具定义中，98个（11.0%）涉及敏感操作（签名、授权、转账、密钥管理），无一受到适当的安全控制保护。

**第三，安全包装器普遍缺失。** 在我们定义的6项Harness安全需求中，没有任何一项在42个Web3仓库中得到充分实现。98个敏感工具无一受到权限约束、沙箱隔离或交易验证策略的保护。

**第四，动态测试验证了静态发现。** 对10个仓库136个工具的5维度动态测试额外发现252个漏洞，验证了参数注入（100%工具易受攻击）和提示注入输出（67.6%）的高流行度。

我们呼吁协议设计者将安全性作为协议核心属性而非可选特性，尤其在Web3场景下：工具应默认实施交易参数验证、签名用户确认和私钥隔离。安全控制不应依赖个体工具开发者的主动实现，而应由协议SDK作为不可旁路的默认行为提供。我们发布的静态分析工具、动态测试框架和数据集旨在支持社区持续监测和改进AI agent工具生态的安全状况。

---

## 参考文献

[1] Anthropic. "Claude 3.5 Computer Use and Tool Use." 2024.

[2] OpenAI. "GPT-4 Technical Report." arXiv 2303.08774, 2023.

[3] Anthropic. "Model Context Protocol Specification." 2024. https://modelcontextprotocol.io/specification

[4] OpenAI. "Function Calling." OpenAI API Documentation, 2023.

[5] LangChain. "Tools and Toolkits." LangChain Documentation, 2024.

[6] Safe. "Safe Modules Documentation." 2023. https://docs.safe.global/advanced/smart-account-modules

[7] ERC-4337. "Account Abstraction Using Alt Mempool." Ethereum EIPs, 2023.

[8] ERC-7579. "Minimal Modular Smart Accounts." Ethereum EIPs, 2024.

[9] Invariant Labs. "MCP Security Analysis: Tool Poisoning Attacks." 2025. https://invariantlabs.ai/blog/mcp-security-analysis

[10] Hou, X., et al. "Security Analysis of MCP-Based Multi-Agent Systems." arXiv, 2025.

[11] Greshake, K., Abdelnabi, S., Mishra, S., Endres, C., Holz, T., and Fritz, M. "Not What You've Signed Up For: Compromising Real-World LLM-Integrated Applications with Indirect Prompt Injection." AISec@CCS, 2023.

[12] Zhan, Q., et al. "Systematic Analysis of Attacks Against LLM-Based Agents." arXiv 2402.08276, 2024.

[13] OWASP. "OWASP Top 10 for Large Language Model Applications." 2025. https://owasp.org/www-project-top-10-for-large-language-model-applications/

[14] Debenedetti, E., et al. "AgentDojo: A Dynamic Environment to Assess Attacks and Defenses for LLM Agents." arXiv 2406.13352, 2024.

[15] Atzei, N., Bartoletti, M., and Cimoli, T. "A Survey of Attacks on Ethereum Smart Contracts (SoK)." POST, 2017.

[16] Chen, J., Xia, X., Lo, D., Grundy, J., Luo, X., and Chen, T. "Defining Smart Contract Defects on Ethereum." IEEE TSE, 2020.

[17] Tsankov, P., Dan, A., Drachsler-Cohen, D., Gervais, A., Buenzli, F., and Vechev, M. "Securify: Practical Security Analysis of Smart Contracts." CCS, 2018.

[18] OWASP. "OWASP API Security Top 10 -- 2023." https://owasp.org/API-Security/

[19] Atlidakis, V., Godefroid, P., and Polishchuk, M. "RESTler: Stateful REST API Fuzzing." ICSE, 2019.

[20] Xi, Z., et al. "AI Agent: A Comprehensive Survey." arXiv 2601.04583, 2025.

[21] Wang, L., et al. "A Survey on Large Language Model Based Autonomous Agents." arXiv 2308.11432, 2024.

[22] Ruan, J., et al. "Identifying the Risks of LM Agents with an LM-Emulated Sandbox." arXiv 2309.15817, 2024.
